# Test-only deps. Runtime deps live in requirements.txt.
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/python -m pytest tests/unit
pytest>=8.0.0
pytest-asyncio>=0.23.0   # repo sets asyncio_mode=auto in pytest.ini
pytest-mock>=3.12.0      # `mocker` fixture, used by tests/unit/test_result_handler.py
